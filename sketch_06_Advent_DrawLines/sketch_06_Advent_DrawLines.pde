import processing.pdf.*;
import java.util.Calendar;

boolean savePDF = false;

void setup() {
  size (1000, 1000); //total size 
  background(255, 0,79); //background colour: pink
  }
  void draw() {
  line(mouseX, mouseY, 500,500);
}
  
  
